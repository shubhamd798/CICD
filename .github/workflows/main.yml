name: DevSecOps Secure CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-scan:
    name: 🔐 Build & Security Scan
    runs-on: ubuntu-latest
    steps:
      - name: ✅ CI trigger works
        run: echo "Workflow running successfully 🚀"
